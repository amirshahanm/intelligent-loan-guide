<!-- LOVABLE:BEGIN -->

> [!IMPORTANT]
> This project is connected to [Lovable](https://lovable.dev). Avoid rewriting
> published git history — force pushing, or rebasing/amending/squashing commits
> that are already pushed — as it rewrites history on Lovable's side and the
> user will likely lose their project history.
>
> Commits you push to the connected branch sync back to Lovable and show up in
> the editor, so keep the branch in a working state.

<!-- LOVABLE:END -->

## TashilRadar implementation authority

Before planning or changing product architecture, read `FOUNDER_LOCK.md`.

`FOUNDER_LOCK.md` is the current Founder-approved product direction and overrides stale historical planning language in `README.md`, including the old `MODE: PLANNING ONLY` instruction.

Implementation is active. Preserve approved deterministic cores and extend the platform according to the Founder Lock rather than reinterpreting TashilRadar as a cash-loan-only product.
